# docs UI (historique)

Copie des fichiers provenant de `services/ui-web/docs/`.

À traiter comme référence historique; la documentation officielle UI doit vivre dans `docs/ui/`
(sans le préfixe `_from-`), puis on pourra retirer cette copie.
